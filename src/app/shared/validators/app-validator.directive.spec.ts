import { AppValidatorDirective } from './app-validator.directive';

describe('AppValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new AppValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
